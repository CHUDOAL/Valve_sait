# Переменные окружения для Render.com

## Необходимые переменные

Для работы приложения на Render.com нужно добавить следующие переменные окружения:

### 1. OpenAI API Key (для ИИ в чате)

**Название:** `OPENAI_API_KEY`

**Значение:** Ваш API ключ от OpenAI

**Как получить:**
1. Зайдите на [platform.openai.com](https://platform.openai.com)
2. Войдите в аккаунт или создайте новый
3. Перейдите в раздел "API keys"
4. Нажмите "Create new secret key"
5. Скопируйте ключ (он показывается только один раз!)

**Пример:** `sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

**Важно:** 
- Храните ключ в секрете, не публикуйте его в репозитории
- Используйте переменные окружения для хранения ключа
- На Render.com добавьте ключ через панель управления

---

## Как добавить переменные на Render.com

### Шаг 1: Откройте настройки сервиса
1. Зайдите на [dashboard.render.com](https://dashboard.render.com)
2. Выберите ваш сервис (valve-portal)
3. Перейдите в раздел "Environment"

### Шаг 2: Добавьте переменную
1. Нажмите "Add Environment Variable"
2. В поле "Key" введите: `OPENAI_API_KEY`
3. В поле "Value" вставьте ваш API ключ от OpenAI
4. Нажмите "Save Changes"

### Шаг 3: Перезапустите сервис
1. После добавления переменной Render автоматически перезапустит сервис
2. Дождитесь завершения деплоя
3. Проверьте работу ИИ в чате

---

## Опциональные переменные

### PORT (автоматически)
Render автоматически устанавливает переменную `PORT`, её не нужно добавлять вручную.

### DATABASE_URL (если используете PostgreSQL)
Если вы хотите использовать PostgreSQL вместо SQLite, добавьте переменную `DATABASE_URL`:
- Формат: `postgresql+asyncpg://user:password@host:port/dbname`
- Render автоматически создаст PostgreSQL базу данных при необходимости

---

## Проверка работы

После добавления переменных:

1. **Проверьте логи:**
   - В разделе "Logs" проверьте, что нет ошибок
   - Должно быть сообщение об успешном запуске

2. **Проверьте ИИ в чате:**
   - Зайдите на страницу чата
   - Нажмите кнопку 🤖 или используйте Ctrl+Enter
   - Отправьте сообщение
   - Должен прийти ответ от ИИ

3. **Если ИИ не работает:**
   - Проверьте, что `OPENAI_API_KEY` правильно добавлен
   - Проверьте, что ключ действителен (не истек)
   - Проверьте баланс на аккаунте OpenAI
   - Посмотрите логи на Render для деталей ошибки

---

## Безопасность

⚠️ **Важно:**
- Никогда не добавляйте API ключи в код
- Используйте только переменные окружения
- Не коммитьте файлы с ключами в Git
- Регулярно обновляйте ключи при необходимости

---

## Стоимость OpenAI API

- **o1-mini:** ~$0.15 за 1M входных токенов, ~$0.60 за 1M выходных токенов
- **gpt-4o-mini:** ~$0.15 за 1M входных токенов, ~$0.60 за 1M выходных токенов

Для корпоративного чата это очень дешево - несколько центов за тысячу сообщений.

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи на Render
2. Убедитесь, что API ключ правильный
3. Проверьте баланс на OpenAI аккаунте
4. Убедитесь, что модель доступна (o1-mini или gpt-4o-mini)

